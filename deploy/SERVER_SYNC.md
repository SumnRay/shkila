# Инструкция для синхронизации на сервере

## ✅ Первый раз (настройка):

```bash
# Подключись к серверу через SSH
ssh root@5.129.223.109

# Перейди в директорию проекта
cd /var/www/shkila

# Убедись что на правильной ветке (alpha3)
git checkout alpha3

# Настрой git для правильной работы pull
git config pull.rebase false

# Обнови код из GitHub
git pull origin alpha3
```

## 🔄 Каждый раз при обновлении:

```bash
cd /var/www/shkila
git pull origin alpha3
```

## 🚀 Полный деплой фронтенда (после git pull):

```bash
cd /var/www/shkila

# Обновить код
git pull origin alpha3

# Перейти в front и собрать
cd front
npm install  # только если добавились новые зависимости
npm run build

# Скопировать собранные файлы
sudo cp -r dist/* /var/www/frontend/

# Установить права
sudo chown -R www-data:www-data /var/www/frontend
sudo chmod -R 755 /var/www/frontend
```

## 📝 Или использовать скрипт (после первого раза):

```bash
cd /var/www/shkila
chmod +x deploy/deploy_frontend.sh
./deploy/deploy_frontend.sh
```

## ⚠️ Важно:

- ✅ База данных MySQL НЕ будет затронута (она на сервере, не в git)
- ✅ `.env` файлы НЕ будут перезаписаны (они в .gitignore)
- ✅ Все настройки сервера останутся нетронутыми

