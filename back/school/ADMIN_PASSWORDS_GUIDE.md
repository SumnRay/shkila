# 📋 Подробное руководство по управлению паролями админов

## 🎯 Ваша текущая конфигурация

У вас настроено **2 админа**:

1. **Root Admin** (вшитый): `kazak_jenya@mail.ru`
   - Создаётся автоматически при миграциях
   - Пароль задаётся через переменную окружения `ROOT_ADMIN_PASSWORD`
   - Имеет `is_superuser=True` (полные права Django)

2. **Второй админ** (из белого списка): `nikitasemenenko3@gmail.com`
   - Создаётся через `/api/auth/admin-login/` при первом входе
   - Пароль задаётся при первом входе через API
   - Также получает `is_superuser=True` (если лимит не исчерпан)

---

## 🔐 Способ 1: Управление через переменные окружения (РЕКОМЕНДУЕТСЯ)

### Для Root Admin (`kazak_jenya@mail.ru`)

#### Windows PowerShell:
```powershell
# Установить пароль для текущей сессии
$env:ROOT_ADMIN_PASSWORD="ВашБезопасныйПароль123!"

# Проверить, что установлено
echo $env:ROOT_ADMIN_PASSWORD

# Затем запустить миграции
cd back\school
python manage.py migrate
```

#### Windows CMD:
```cmd
set ROOT_ADMIN_PASSWORD=ВашБезопасныйПароль123!
python manage.py migrate
```

#### Linux/Mac:
```bash
export ROOT_ADMIN_PASSWORD="ВашБезопасныйПароль123!"
python manage.py migrate
```

#### Постоянная установка (Windows):
1. Откройте "Переменные среды" (Environment Variables)
2. Добавьте новую переменную:
   - Имя: `ROOT_ADMIN_PASSWORD`
   - Значение: `ВашБезопасныйПароль123!`

#### Постоянная установка (Linux/Mac):
Добавьте в `~/.bashrc` или `~/.zshrc`:
```bash
export ROOT_ADMIN_PASSWORD="ВашБезопасныйПароль123!"
```

---

## 🔐 Способ 2: Создание/изменение паролей через Django shell

### Для Root Admin:

```bash
python manage.py shell
```

```python
from accounts.models import User
from django.conf import settings

# Найти root admin
root_email = settings.ROOT_ADMIN_EMAIL
admin = User.objects.get(email=root_email)

# Установить новый пароль
admin.set_password("НовыйБезопасныйПароль123!")
admin.save()

print(f"✓ Пароль для {root_email} изменён")
```

### Для второго админа:

```python
from accounts.models import User

# Найти второго админа
admin2 = User.objects.get(email="nikitasemenenko3@gmail.com")

# Установить новый пароль
admin2.set_password("НовыйПарольДляВторогоАдмина123!")
admin2.save()

print(f"✓ Пароль для {admin2.email} изменён")
```

### Изменить пароли обоих сразу:

```python
from accounts.models import User
from django.conf import settings

# Root Admin
root_admin = User.objects.get(email=settings.ROOT_ADMIN_EMAIL)
root_admin.set_password("ПарольПервогоАдмина123!")
root_admin.save()

# Второй админ
admin2 = User.objects.get(email="nikitasemenenko3@gmail.com")
admin2.set_password("ПарольВторогоАдмина123!")
admin2.save()

print("✓ Пароли обоих админов изменены")
```

---

## 🔐 Способ 3: Создание второго админа через API (первый раз)

Если второй админ ещё не создан, используйте `/api/auth/admin-login/`:

### Через curl:
```bash
curl -X POST http://localhost:8000/api/auth/admin-login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "nikitasemenenko3@gmail.com",
    "password": "ПарольПриСоздании123!"
  }'
```

### Через Python requests:
```python
import requests

response = requests.post(
    "http://localhost:8000/api/auth/admin-login/",
    json={
        "email": "nikitasemenenko3@gmail.com",
        "password": "ПарольПриСоздании123!"
    }
)

print(response.json())
```

**Важно:** При первом вызове этого API пользователь будет создан с указанным паролем.

---

## 🔐 Способ 4: Управление через Django Admin панель

1. Войдите как root admin в `/django-admin/`
2. Перейдите в "Пользователи" → выберите нужного админа
3. Нажмите "Изменить пароль" (Change password)
4. Введите новый пароль дважды
5. Сохраните

---

## 🔐 Способ 5: Создание скрипта для управления паролями

Создайте файл `manage_admins.py` в корне проекта:

```python
#!/usr/bin/env python
"""
Скрипт для управления паролями админов
Использование: python manage_admins.py
"""
import os
import sys
import django

# Настройка Django
sys.path.append(os.path.dirname(os.path.abspath(__file__)))
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'school.settings')
django.setup()

from accounts.models import User
from django.conf import settings

def set_admin_password(email, password):
    """Установить пароль для админа"""
    try:
        user = User.objects.get(email=email)
        user.set_password(password)
        user.save()
        print(f"✓ Пароль для {email} успешно изменён")
        return True
    except User.DoesNotExist:
        print(f"✗ Пользователь {email} не найден")
        return False

def main():
    print("=== Управление паролями админов ===\n")
    
    # Root Admin
    root_email = settings.ROOT_ADMIN_EMAIL
    root_password = input(f"Введите новый пароль для {root_email}: ")
    set_admin_password(root_email, root_password)
    
    # Второй админ
    admin2_email = "nikitasemenenko3@gmail.com"
    admin2_password = input(f"Введите новый пароль для {admin2_email}: ")
    set_admin_password(admin2_email, admin2_password)
    
    print("\n✓ Готово!")

if __name__ == "__main__":
    main()
```

Запуск:
```bash
cd back/school
python manage_admins.py
```

---

## 📝 Рекомендуемый workflow для первого запуска

### Шаг 1: Установите пароль для Root Admin через переменную окружения

```powershell
# Windows PowerShell
$env:ROOT_ADMIN_PASSWORD="RootAdmin123!@#"
```

### Шаг 2: Выполните миграции

```bash
cd back\school
python manage.py migrate
```

Это создаст root admin автоматически с паролем из переменной окружения.

### Шаг 3: Создайте второго админа через API

```bash
curl -X POST http://localhost:8000/api/auth/admin-login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "nikitasemenenko3@gmail.com",
    "password": "Admin2Password123!@#"
  }'
```

### Шаг 4: Проверьте создание обоих админов

```bash
python manage.py shell
```

```python
from accounts.models import User
from django.conf import settings

# Проверка root admin
root = User.objects.get(email=settings.ROOT_ADMIN_EMAIL)
print(f"Root Admin: {root.email}, is_superuser: {root.is_superuser}, role: {root.role}")

# Проверка второго админа
admin2 = User.objects.get(email="nikitasemenenko3@gmail.com")
print(f"Admin 2: {admin2.email}, is_superuser: {admin2.is_superuser}, role: {admin2.role}")
```

---

## 🔄 Изменение паролей после создания

### Вариант A: Через Django shell (быстро)

```bash
python manage.py shell
```

```python
from accounts.models import User

# Изменить пароль root admin
User.objects.get(email="kazak_jenya@mail.ru").set_password("НовыйПароль1")
User.objects.get(email="kazak_jenya@mail.ru").save()

# Изменить пароль второго админа
User.objects.get(email="nikitasemenenko3@gmail.com").set_password("НовыйПароль2")
User.objects.get(email="nikitasemenenko3@gmail.com").save()
```

### Вариант B: Через Django admin панель

1. Войдите в `/django-admin/` как один из админов
2. Пользователи → выберите админа → "Изменить пароль"

### Вариант C: Через API (если знаете старый пароль)

```bash
# Войдите с текущим паролем
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "kazak_jenya@mail.ru",
    "password": "СтарыйПароль"
  }'

# Получите токен, затем используйте его для изменения пароля
# (если у вас есть эндпоинт для смены пароля)
```

---

## 🔒 Безопасность паролей

### Требования к паролям (настроены в Django):

- Минимум 8 символов (рекомендуется 12+)
- Не должен быть похож на email или имя пользователя
- Не должен быть в списке распространённых паролей
- Должен содержать буквы и цифры (рекомендуется также символы)

### Примеры хороших паролей:

✅ `Admin2024!Secure#`
✅ `Kazak_Jenya_Admin_2024!`
✅ `Nikita_Semenenko_Admin_2024!`
✅ `MySchool_Admin_2024!@#`

### Примеры плохих паролей:

❌ `admin123`
❌ `password`
❌ `12345678`
❌ `kazak_jenya` (похож на email)

---

## 🚨 Важные замечания

1. **Root Admin** создаётся автоматически только при первом запуске миграций
2. Если root admin уже существует, сигнал не изменит его пароль (только права)
3. **Второй админ** создаётся только при первом вызове `/api/auth/admin-login/`
4. Лимит суперпользователей из белого списка: **2 человека**
5. После создания обязательно смените дефолтные пароли!

---

## 📋 Чек-лист настройки

- [ ] Установлена переменная окружения `ROOT_ADMIN_PASSWORD`
- [ ] Выполнены миграции (`python manage.py migrate`)
- [ ] Root admin создан и проверен
- [ ] Второй админ создан через `/api/auth/admin-login/`
- [ ] Оба админа могут войти через `/api/auth/login/`
- [ ] Оба админа могут войти в Django admin (`/django-admin/`)
- [ ] Пароли изменены с дефолтных на безопасные
- [ ] Пароли сохранены в безопасном месте (менеджер паролей)

---

## 🆘 Решение проблем

### Проблема: "Пользователь не найден"

**Решение:** Убедитесь, что:
- Для root admin: выполнены миграции
- Для второго админа: вызван `/api/auth/admin-login/`

### Проблема: "Неверный пароль"

**Решение:** 
- Проверьте правильность пароля
- Сбросьте пароль через Django shell (см. Способ 2)

### Проблема: "Лимит админов исчерпан"

**Решение:** 
- Уже создано 2 суперпользователя из белого списка
- Удалите одного из существующих или создайте админа без `is_superuser=True`

### Проблема: Пароль не соответствует требованиям

**Решение:**
- Используйте более сложный пароль (12+ символов, буквы, цифры, символы)
- Или временно отключите валидаторы в `settings.py` (НЕ рекомендуется для продакшена)

---

## 📞 Быстрая справка

**Создать/изменить пароль через shell:**
```python
User.objects.get(email="email@example.com").set_password("новый_пароль")
User.objects.get(email="email@example.com").save()
```

**Проверить существование админа:**
```python
User.objects.filter(email="email@example.com", role="ADMIN").exists()
```

**Список всех админов:**
```python
User.objects.filter(role="ADMIN").values('email', 'is_superuser', 'is_staff')
```













