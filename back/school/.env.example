# Django Settings
DEBUG=False
SECRET_KEY=REPLACE_ME_WITH_SECURE_RANDOM_STRING

# Allowed Hosts (comma-separated, no spaces)
ALLOWED_HOSTS=yourdomain.ru,www.yourdomain.ru,127.0.0.1

# Database Configuration
DB_NAME=flare
DB_USER=flare_user
DB_PASSWORD=REPLACE_ME
DB_HOST=127.0.0.1
DB_PORT=3306

# CORS Origins (comma-separated, no spaces)
CORS_ORIGINS=https://yourdomain.ru,http://localhost:5173

# Admin Configuration
ROOT_ADMIN_EMAIL=admin@example.com
ADMIN_SEED_EMAILS=admin@example.com,second@example.com

# Optional: Root Admin Password (if not set, default will be used)
# ROOT_ADMIN_PASSWORD=REPLACE_ME
