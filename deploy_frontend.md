# Инструкция по деплою фронтенда на TimeWeb (через GitHub)

## 🚀 Быстрый способ (через Git - РЕКОМЕНДУЕТСЯ)

Так как у тебя проект уже на GitHub (`https://github.com/SumnRay/shkila.git`), используй этот способ!

### Процесс обновления:

**1. Локально: закоммить и запушить изменения**
```bash
git add .
git commit -m "Обновление фронтенда: новый логотип"
git push origin main  # или master, в зависимости от твоей ветки
```

**2. На сервере: обновить и собрать**

Подключись к серверу через SSH и выполни:
```bash
# Перейди в директорию проекта (предполагается что проект клонирован в /var/www/shkila)
cd /var/www/shkila

# Обновить код из GitHub
git pull origin main  # или master

# Перейти в папку front и собрать
cd front
npm install  # только если добавились новые зависимости
npm run build

# Скопировать собранные файлы в директорию для веб-сервера
sudo cp -r dist/* /var/www/frontend/

# Установить правильные права
sudo chown -R www-data:www-data /var/www/frontend
sudo chmod -R 755 /var/www/frontend
```

### Один скрипт для всего (на сервере)

Создай на сервере файл `/var/www/shkila/deploy_frontend.sh`:
```bash
#!/bin/bash
cd /var/www/shkila
git pull origin main
cd front
npm install
npm run build
sudo cp -r dist/* /var/www/frontend/
sudo chown -R www-data:www-data /var/www/frontend
sudo chmod -R 755 /var/www/frontend
echo "✅ Фронтенд обновлен!"
```

Сделай его исполняемым и запускай:
```bash
sudo chmod +x /var/www/shkila/deploy_frontend.sh
/var/www/shkila/deploy_frontend.sh
```

---

## Вариант 2: Деплой через SSH (старый способ, если Git не настроен на сервере)

### Шаги:

1. **Локально: собрать фронтенд**
   ```bash
   cd front
   npm run build
   ```
   После этого в папке `front/dist/` будут готовые файлы для деплоя.

2. **Подключиться к серверу через SSH**
   - Войди в консоль TimeWeb и открой SSH доступ к серверу
   - Используй SSH-клиент (PuTTY, WinSCP, или встроенный в Windows Terminal)

3. **Загрузить файлы на сервер**

   **Вариант A: Через SCP (из командной строки локально)**
   ```bash
   # Из корня проекта (C:\shkila)
   scp -r front/dist/* user@your-server-ip:/var/www/frontend/
   ```
   Замени `user@your-server-ip` на реальные данные доступа TimeWeb

   **Вариант B: Через WinSCP (Windows GUI)**
   - Подключись к серверу через WinSCP
   - Скопируй все файлы из `front/dist/` в `/var/www/frontend/` на сервере

   **Вариант C: Через SFTP клиент (FileZilla)**
   - Подключись по SFTP
   - Загрузи все файлы из `front/dist/` в `/var/www/frontend/`

4. **Установить правильные права доступа**
   ```bash
   sudo chown -R www-data:www-data /var/www/frontend
   sudo chmod -R 755 /var/www/frontend
   ```

5. **Очистить кэш браузера (если нужно)**
   - В Nginx можно добавить заголовки для отключения кэша в dev, но обычно не требуется

## Вариант 2: Деплой через файловый менеджер TimeWeb

1. **Локально: собрать фронтенд**
   ```bash
   cd front
   npm run build
   ```

2. **Запаковать dist в архив**
   - Создай ZIP архив папки `front/dist/`
   - Назови его например `frontend-build.zip`

3. **В консоли TimeWeb:**
   - Зайди в файловый менеджер
   - Перейди в `/var/www/frontend/`
   - Удали старые файлы (или сделай backup)
   - Загрузи ZIP архив
   - Распакуй его

4. **Установить права доступа** (через SSH или консоль TimeWeb)
   ```bash
   sudo chown -R www-data:www-data /var/www/frontend
   sudo chmod -R 755 /var/www/frontend
   ```

## Быстрая команда для обновления (если уже настроен SSH)

Если у тебя уже настроен SSH доступ, можно сделать так:

```bash
# Локально
cd front
npm run build
cd dist
tar -czf ../frontend-build.tar.gz *

# Затем на сервере через SSH:
cd /var/www/frontend
sudo rm -rf *  # Удалить старые файлы (ОСТОРОЖНО!)
sudo tar -xzf /path/to/frontend-build.tar.gz
sudo chown -R www-data:www-data /var/www/frontend
sudo chmod -R 755 /var/www/frontend
```

## Проверка после деплоя

1. Открой сайт в браузере
2. Проверь, что фронтенд загружается
3. Проверь в DevTools (F12) → Network, что все файлы загружаются (статус 200)
4. Проверь Console на наличие ошибок

## Важные замечания

- ⚠️ **Всегда делай backup** перед обновлением: `sudo cp -r /var/www/frontend /var/www/frontend.backup`
- ⚠️ **Проверь путь в Nginx**: конфиг говорит что фронтенд в `/var/www/frontend`, убедись что это так
- ⚠️ **После обновления** может понадобиться очистить кэш браузера (Ctrl+Shift+R или Ctrl+F5)
- ⚠️ Если используешь **HTTPS**, убедись что SSL сертификат настроен правильно

## Если что-то пошло не так

1. Проверь логи Nginx: `sudo tail -f /var/log/nginx/error.log`
2. Проверь что файлы на месте: `ls -la /var/www/frontend`
3. Проверь права доступа: `ls -la /var/www/frontend | head -5`
4. Перезагрузи Nginx: `sudo systemctl reload nginx`

